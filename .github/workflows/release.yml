name: PyPI Release

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  pypi-publish:
    name: Upload release to PyPI
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/fate-casino
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - name: Install Hatch
        run: pipx install hatch
      - uses: actions/cache@v5
        with:
          path: ~/.cache/hatch
          key: ${{ runner.os }}-hatch
          restore-keys: ${{ runner.os }}-hatch
      - name: Build package
        run: hatch build
      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
